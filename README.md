# HSL Transitdata Commons library

This codebase contains common code to be shared between Transitdata-projects. 

Usage:

- Build

  `mvn clean package`
  
- Deploy to local artefactory, for other projects to access it

  `mvn clean install`